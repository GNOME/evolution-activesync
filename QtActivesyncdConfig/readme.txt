The QtActivesyncdConfig project requires Qt in order to build.
It has been built with Qt 4.7.3 but should work with much older
versions too as it doesn't use any new features.

You can download the Qt SDK (including all required libraries
and the Qt Creator IDE) from http://qt.nokia.com/downloads.
